# SolarFlow — Production Launch Checklist

Filled in with real, verified status as of Step 86 (2026-08-14). `[x]`
means verified against actual code/test results this session — not
assumed. Items marked **⚠️ CONFIRM IN RENDER DASHBOARD** are things only
visible in Render's live configuration, not in this repo — this report
can't see them, so it doesn't claim to have verified them.

## Pre-Launch

- [x] All P0/P1 bugs fixed — see `docs/operations/qa-report.md`'s defect classification: Critical 0, High 0
- [x] API Contract finalized and documented — `docs/api-contract.md`; live OpenAPI schema at `/docs`
- [x] OpenAPI/Swagger complete — auto-generated by FastAPI from every router's Pydantic schemas
- [x] Unit tests passing — 219/219 (see `docs/operations/qa-report.md`)
- [x] Integration tests passing — same suite, no separate unit/integration split; every test hits a real Postgres DB
- [ ] E2E tests passing — N/A as a separate suite: critical journeys (register→login→core action) are tested at the API level throughout `tests/`, but no browser-driven E2E exists (frontend is a separate repo)
- [x] Security tests passing — SAST (bandit, 0 medium+ findings), dependency scan (pip-audit, clean), secrets scan (clean baseline) — Step 85
- [x] Tenant isolation verified — `tests/integration/test_tenant_isolation.py`, organization_id scoping enforced throughout
- [x] Load test completed — Step 84's real load test found and fixed a 14-16x P95 regression

## Environment

- [ ] Production DATABASE_URL configured — **⚠️ CONFIRM IN RENDER DASHBOARD** (code reads it from env, doesn't set it)
- [ ] JWT_SECRET rotated (not dev value) — **⚠️ CONFIRM IN RENDER DASHBOARD**
- [ ] CORS_ALLOWED_ORIGINS set to production frontend URL — **⚠️ CONFIRM IN RENDER DASHBOARD** (code fails closed — empty env var means zero origins allowed, not a wildcard — so a misconfiguration here breaks CORS loudly, not silently)
- [ ] APP_ENV=production — **⚠️ CONFIRM IN RENDER DASHBOARD**
- [ ] DEBUG=false — **⚠️ CONFIRM IN RENDER DASHBOARD** (low risk either way: `DEBUG` is read into `settings.DEBUG` but never passed to `FastAPI(debug=...)`, confirmed in `app/main.py` — Starlette's debug/traceback mode is never enabled regardless of this env var)
- [ ] Email provider configured — **N/A, not built yet**: registration/verification emails are logged (`[STUB EMAIL]`), not sent; documented in `docs/policies/vendor-policy.md` as a real, known gap, not a launch blocker for functionality that doesn't depend on email delivery
- [ ] Payment provider configured — **N/A**: no payment gateway integration exists anywhere in this codebase (confirmed via grep for stripe/paypal/etc.)

## Database

- [x] Migrations applied successfully — 61 migrations, all applying cleanly on every step this session; CI now runs `alembic upgrade head` against a real Postgres container on every push
- [x] Backup configured — Render's managed Postgres automatic daily backups + PITR (`docs/operations/disaster-recovery.md`)
- [ ] Backup restore tested — **known, documented gap** (Step 83/85): no Render API access to spin up an isolated restore environment from this session; the manual quarterly drill process is operationalized (real RPO/RTO targets, drill tracking) but an actual restore has not been executed and verified end-to-end
- [x] RPO/RTO defined — real targets seeded per service (API 15/30min, Database 60/60min, Billing 15/60min, etc. — Step 83)
- [x] Connection pool sized appropriately — tuned from real load-test data (20/30, Step 84); verify against the actual Render Postgres plan's `max_connections` before deploy (documented in `docs/operations/performance-scalability-report.md`)

## Deployment

- [ ] Render Web Service configured — **⚠️ CONFIRM IN RENDER DASHBOARD**
- [x] Start command: `alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT` — documented in `docs/deployment-guide.md`; the migration step now has a `lock_timeout`/`statement_timeout` (Step 84, added after a real deploy hang) so a future lock conflict fails fast instead of silently burning the whole deploy window
- [x] Health check path: `/health` — pure liveness, no DB dependency, so a momentary DB blip doesn't cause Render to recycle a healthy process
- [ ] Auto-deploy from main branch enabled — **⚠️ CONFIRM IN RENDER DASHBOARD** (this is Render's own polling integration, not something in this repo — confirmed CI's `.github/workflows/ci.yml` has no deploy step, so Render's dashboard setting is the only deploy trigger)
- [x] Rollback strategy documented — `docs/release-process.md`'s Rollback Plan section; Render dashboard → select previous deploy → redeploy

## Post-Deploy (run these after every deploy, including the next one)

- [ ] `GET /health` returns 200
- [ ] `GET /health/ready` returns ready
- [ ] Swagger `/docs` accessible
- [ ] Login works
- [ ] Dashboard loads data
- [ ] No 500 errors in logs

## Monitoring

- [x] Error tracking enabled — Sentry integration (`app/core/error_tracking.py`), inert unless `SENTRY_DSN` set — **⚠️ CONFIRM `SENTRY_DSN` IS SET IN RENDER DASHBOARD** if error tracking is expected to be active
- [x] Request logging enabled — every request logged with request_id/trace_id (`RequestLoggingMiddleware`, Step 28)
- [x] Security events tracking active — `log_security_event` wired into real call sites since Step 82 (login, permission denials, token reuse, device/API-key auth failures)
- [x] Alert system operational — 6 real alerting jobs registered (security correlation, budget thresholds, performance P95/error-rate/capacity, KPI alerts, forecast drift) — full list in `docs/production-readiness-report.md`
- [x] Backup monitoring — Render manages backups directly; `BackupRecord` table exists but the app doesn't independently verify Render's own backup success (documented gap, same as backup restore above)

## Documentation

- [x] API documentation published — OpenAPI at `/docs`, `docs/api-contract.md`
- [x] Deployment guide complete — `docs/deployment-guide.md`
- [x] Rollback guide complete — `docs/release-process.md`
- [x] Incident response guide complete — `docs/operations/incidents.md` + `docs/policies/business-continuity-plan.md` (Step 83)
